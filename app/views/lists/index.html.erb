<h1>Lists#index</h1>
<ul>
  <% @lists.each do |list| %>
    <li><%= cl_image_tag @list.photo.key, height: 150, width: 200, crop: :fill <%= link_to list.name, list_path(list)%></li>
  <% end %>
</ul>
<%= link_to "new list", new_list_path %>
